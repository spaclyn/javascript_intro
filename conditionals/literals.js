// we use the type function to create a instance
let favNum = String(5) // string
let favNum2 = Number('5.0') // number
let favNum2 = new Number('5.0') // a look ahead -> this is a "type constructor"

// when we just write the js notation of the type ie ...
let litStr = "this is a string"
let litNum = 5.5
let litArr = [1, 2]
let litObj = {
    name: 'atiyah',
    age: 25
}